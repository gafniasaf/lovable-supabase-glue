# Vercel Environment (Production)
# Copy these into Vercel Project Settings → Environment Variables
# Public (NEXT_PUBLIC_*) are exposed to the browser. Others are server-only secrets.

NODE_ENV=production
TEST_MODE=0

# Feature flags
FEATURES_EXPERTFOLIO=1

# Public base URL
NEXT_PUBLIC_BASE_URL=https://your-domain.tld

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_ANON_KEY
# Optional (server-only): used only if server-side services need elevated access
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY

# File storage
NEXT_PUBLIC_UPLOAD_BUCKET=public

# Security
CSRF_DOUBLE_SUBMIT=1
MVP_PROD_GUARD=0

# Observability / internal metrics endpoint guard
# Set a random token and provide it in x-metrics-token when querying /api/internal/metrics
METRICS_TOKEN=REPLACE_WITH_RANDOM_HEX

# Optional rate limits / tuning (leave unset to use defaults)
# EF_CREATE_RATE_LIMIT=60
# EF_CREATE_RATE_WINDOW_MS=60000

# Runtime V2 (if used by interactive providers)
# RUNTIME_API_V2=1

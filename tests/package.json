{
  "name": "tests",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "@axe-core/playwright": "^4.9.1",
    "@playwright/test": "^1.46.0",
    "@testing-library/jest-dom": "^6.4.8",
    "@testing-library/react": "^16.0.0",
    "@testing-library/user-event": "^14.5.2",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "cypress": "^13.13.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.5",
    "msw": "^2.10.5",
    "next-router-mock": "^0.9.13",
    "start-server-and-test": "^2.0.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.5.4",
    "wait-on": "^7.0.1"
  },
  "scripts": {
    "presmoke": "powershell -NoProfile -ExecutionPolicy Bypass -File ./e2e/kill-web.ps1 && powershell -NoProfile -ExecutionPolicy Bypass -File ./e2e/kill-port.ps1 -Port 3030",
    "test": "jest --passWithNoTests",
    "test:coverage": "jest --coverage",
    "test:critical": "jest --findRelatedTests ../apps/web/src/server/services/**/*.ts ../apps/web/src/app/api/**/*.ts",
    "test:e2e": "playwright test -c ./e2e/playwright.config.ts",
    "smoke": "concurrently -k --success=first \"wait-on -t 120000 tcp:3030 && cross-env PW_NO_SERVER=1 PW_TEST_AUTH=teacher PLAYWRIGHT_BASE_URL=http://localhost:3030 playwright test -c ./e2e/playwright.config.ts -g @smoke --reporter=list\" \"cross-env WEB_PORT=3030 NEXT_PUBLIC_BASE_URL=http://localhost:3030 PLAYWRIGHT_BASE_URL=http://localhost:3030 npm --prefix ../apps/web run dev:e2e\"",
    "smoke-test": "npm run smoke",
    "cy:open": "concurrently -k -s last \"npm --prefix ../apps/web run dev:e2e\" \"wait-on -t 120000 tcp:3030 && cypress open --config baseUrl=http://localhost:3030\"",
    "test:cypress": "concurrently -k -s last \"npm --prefix ../apps/web run dev:e2e\" \"wait-on -t 120000 tcp:3030 && cypress run --browser=chrome --config baseUrl=http://localhost:3030\"",
    "test:cypress:attach": "wait-on -t 120000 tcp:3030 && cypress run --browser=chrome --config baseUrl=http://localhost:3030",
    "test:cypress:quick": "cypress run --browser=chrome --config baseUrl=http://localhost:3030"
  },
  "dependencies": {
    "@types/jest": "^30.0.0",
    "jose": "^6.0.12",
    "zod": "^3.23.8"
  }
}
